import React from "react";
import styles from "./ShoppingList.module.scss";

const ShoppingList = (props) => {
  const {foodCategory, weight} = props.items
  return (
    <>
      <p></p>
    </>
  );
};

export default ShoppingList;
